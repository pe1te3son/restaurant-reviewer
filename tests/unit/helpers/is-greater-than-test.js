import { isGreaterThan } from 'restaurant-reviewer/helpers/is-greater-than';
import { module, test } from 'qunit';

module('Unit | Helper | is greater than');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isGreaterThan([42]);
  assert.ok(result);
});
