import { priceCategory } from 'restaurant-reviewer/helpers/price-category';
import { module, test } from 'qunit';

module('Unit | Helper | price category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceCategory([42]);
  assert.ok(result);
});
