import { bestPhoto } from 'restaurant-reviewer/helpers/best-photo';
import { module, test } from 'qunit';

module('Unit | Helper | best photo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bestPhoto([42]);
  assert.ok(result);
});
